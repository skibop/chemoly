[build]
  command = "npm ci --legacy-peer-deps && npm run build"
  publish = ".next" # or ".next" if needed

[[plugins]]
  package = "@netlify/plugin-nextjs"

[context.production.environment]
  NODE_VERSION = "18"  # Ensure this matches the version you're using locally